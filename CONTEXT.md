# Rest

一个常驻菜单栏、按计划打断你的休息提醒工具。本 fork 的**用户界面是中文**；代码、
注释、上游文档保持英文。

本词表是中文措辞的唯一依据。所有用户可见文案必须使用这里的规范词 —— 同一个概念
不允许出现第二种叫法。`_避免_` 列出的是曾经考虑但已否决的说法。

## 休息类型

**短休息**（Micro break）：
频繁而短促的打断，几十秒，每小时若干次。
_避免_：微休息、小憩

**长休息**（Long break）：
稀疏而较长的打断，数分钟，每天几次。唯一会附带**杂事**提示的类型。
_避免_：大休息、长时休息

**就寝提醒**（Bedtime reminder）：
每天一次、提示你该收工睡觉的推送。代码内部是 `Sleep` 这一 break kind。
_避免_：睡眠提醒、休眠提醒

## 休息的投递方式

**休息界面**：
休息触发时出现在屏幕上的那个东西，不论尺寸。**全屏遮罩**和**窗口模式**都由它渲染；
**仅系统通知**下它不存在。
_避免_：遮罩层、休息窗口、遮屏

**全屏遮罩**（Full-screen overlay）：
覆盖整块屏幕的投递方式。它的不透明度可调，所以可能是半透的 —— 半透也仍然算全屏遮罩。
_避免_：遮屏、全屏模式

**窗口模式**（Windowed）：
显示一个居中窗口的投递方式，大小是屏幕的一个比例。**不覆盖屏幕**。
_避免_：小窗模式

**仅系统通知**（Notification only）：
只发一条系统通知、完全不显示**休息界面**的投递方式。
_避免_：通知模式

## 用户对休息做的操作

**推迟**（Postpone）：
把已到时或正在进行的休息往后挪，稍后重新触发。休息仍然会发生。
_避免_：延后、稍后提醒、贪睡

**跳过**（Skip）：
直接取消这一次休息。与**推迟**（只是延后）相区分，也与**拦截**（由 app 决定而非用户）相区分。
_避免_：忽略、略过

**暂停** / **恢复**（Pause / Resume）：
在选定的时长内停掉整个调度，之后重新开始。作用于调度器，而非某一次休息。
_避免_：停止、挂起

## 休息没有发生的情况

**拦截**（Suppression）：
app 自行决定不打断你 —— 因为不在**活动时段**、系统开着勿扰、摄像头占用、正在播视频、
已**暂停**，或被**插件**否决。用户没有做任何操作；与**跳过**相对。
_避免_：抑制、避让、屏蔽、静默

**活动时段**（Active hours）：
每天允许触发休息的时间窗口。
_避免_：工作时段、生效时间

**严格模式**（Strict mode）：
剥夺用户**跳过**和**推迟**能力的设置。
_避免_：强制模式

## 休息中的内容

**健康提示**（Hint）：
显示在**休息界面**上的一句短建议，休息期间轮播。内部分为身体类和心理类两个池子。
_避免_：提示语、小贴士、健康建议

**引导流程**（Routine）：
一组有序的引导指令，一步步带用户完成一次休息。
_避免_：例程、流程、例行程序

**杂事**（Chore）：
用户自己填的现实待办，在**长休息**时浮出，让休息顺便把事办了。
_避免_：家务、待办、任务

**内容包**（Content pack）：
成套加载的一批**健康提示**和**引导流程**。
_避免_：素材包、内容集

## 组织与观测

**情景**（Profile）：
一套具名、可切换的设置 —— 比如一套用于工作、一套用于居家。同一时刻只有一套生效。
_避免_：配置文件、方案、场景、模式

**统计**（Insights）：
汇报休息历史的标签页：次数、按小时和星期的分布、**拦截**原因、**推迟**率。
_避免_：洞察、数据、分析

**屏幕时间**（Screen time）：
当天在这台机器前的累计活跃时长，独立于休息单独统计。
_避免_：使用时长、在屏时间

**钩子**（Hook）：
用户配置的、在休息生命周期事件上执行的命令。
_避免_：挂钩、回调

**设置窗口**：
承载全部设置的那个窗口，从托盘菜单打开。与**休息界面**是两个独立的窗口，
自定义 CSS 同时作用于两者。
_避免_：主界面、偏好设置、主窗口
