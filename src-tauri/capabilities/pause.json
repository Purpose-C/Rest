{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pause",
  "description": "Minimal capability for the tray-launched Pause picker window. Only needs IPC to call pause commands and close itself; no dialog/notification/window-management permissions.",
  "windows": ["pause"],
  "permissions": ["core:default", "core:event:default", "log:default"]
}
