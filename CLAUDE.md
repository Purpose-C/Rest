# Rest

Personal fork of [drmowinckels/entracte](https://github.com/drmowinckels/entracte) —
cross-platform break reminder app (Tauri 2 + React + Rust), maintained for personal use.

Upstream's agent guide is at [.github/AGENTS.md](.github/AGENTS.md): stack, file-by-file
responsibilities under `src-tauri/src/`, platform support matrix, scheduler internals.

## Agent skills

### Issue tracker

Issues live as markdown files under `.scratch/<feature>/` in this repo. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, used verbatim. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
